// The icon set lives in the shared kit now (its set is a superset of Daffa's).
// Re-exported so every existing `@/lib/icons` import keeps working.
export { iconPaths, type IconName } from '@mnshahawy/daffa-console-ui'
